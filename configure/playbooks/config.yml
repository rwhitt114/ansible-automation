- hosts: localhost
  connection: local
  vars:
    avi_config_file: '{{file}}'
    avi_creds_file: ../credentials
  roles:
    - role: avinetworks.avisdk
    - role: avinetworks.aviconfig
  gather_facts: no
